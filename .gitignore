# --- secrets ---------------------------------------------------------------
.env
.env.local
.env.*.local

# --- python ----------------------------------------------------------------
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# --- data (transcripts + DBs) ----------------------------------------------
# Excluded by default. Public repo policy: we do not republish CT's content.
# Backend hosts run their own `yti ingest` after deploy. See README.
data/*.db
data/*.db-journal
data/raw/

# --- frontend --------------------------------------------------------------
frontend/node_modules/
frontend/.next/
frontend/.vercel/
frontend/*.tsbuildinfo

# --- editor / OS -----------------------------------------------------------
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# --- reference screenshots (personal design refs, not source) --------------
2026-*.png
*.screenshot.png
